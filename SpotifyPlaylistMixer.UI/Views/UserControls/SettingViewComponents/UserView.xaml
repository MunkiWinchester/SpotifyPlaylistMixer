<UserControl x:Class="SpotifyPlaylistMixer.UI.Views.UserControls.SettingViewComponents.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:userControls="clr-namespace:SpotifyPlaylistMixer.UI.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0"  Grid.Row="0" Grid.ColumnSpan="3" Content="User:" FontWeight="Bold"/>
        <Label Grid.Column="0"  Grid.Row="1" Content="Identifier:"/>
        <Label Grid.Column="2"  Grid.Row="1" Content="Name:"/>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="2" Text="{Binding Identifier}" />
        <TextBox Grid.Column="3" Grid.Row="1" Margin="2"  Text="{Binding Name}" />
    </Grid>
</UserControl>
