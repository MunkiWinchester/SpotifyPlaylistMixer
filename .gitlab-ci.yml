image: didstopia/msbuild:latest
stages:
  - build

before_script:
  - nuget restore

build:
 stage: build
 script:
  - msbuild.exe SpotifyPlaylistMixer.sln /t:ReBuild /p:Configuration=Release;Platform="Any CPU"